module param {
    const hazards: boolean;
    const specific_reduction: boolean;
    const additional_reduction: boolean;
    const store_buff: boolean;
    
    const bmc_depth: integer = 38;
    assume(hazards);
    assume(specific_reduction);
    assume(!additional_reduction);
    assume(!store_buff);
    const max_stalling: integer = 3;
}
