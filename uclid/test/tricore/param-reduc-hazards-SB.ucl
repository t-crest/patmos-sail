module param {
    var hazards: boolean;
    var additional_reduction: boolean;
    var store_buff: boolean;
    
    const bmc_depth: integer = 60; // in progress
    
    init {
        hazards = true;
        additional_reduction = true;
        store_buff = true;
    }
}
